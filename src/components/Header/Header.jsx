import React from 'react';
import { StyledHeader } from './StyledHeader';

const Header = () => {
    return (
        <StyledHeader>
            <h1>MY TODO LIST</h1>
        </StyledHeader>
    );
};

export default Header;